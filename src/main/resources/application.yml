server:
  port: 7888

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/whiboxkey?characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql

jwt:
  header: "Authorization"
  tokenPrefix: ""
  secret: "scnucrypto511"
  expireTime: 10512000  #20年有效期：60*24*365*20